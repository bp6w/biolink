# assets

Drop images and audio here. Reference them from `config.js` like `assets/photo.jpg`.

Spaces in filenames work but they’re annoying in URLs — dashes are easier.

If you use the file picker on `settings.html`, it fills in `assets/<whatever you picked>` — you still need to copy that file into this folder before pushing, the browser can’t do that for you.
